extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
      li
        h3= message.name
        p= message.message
        p Date: #{message.date}

  br

  a(href='http://localhost:3000/new') Go To Form

